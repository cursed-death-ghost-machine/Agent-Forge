# Environment Variables for Chimera CLI
# Copy this to .env and modify as needed
# These values override config.toml settings

# Pollinations.ai Configuration
# LLM_API_BASE_URL=https://text.pollinations.ai/openai
# LLM_MODEL_NAME=openai

# API Keys for Pollinations.ai (comma-separated for rotation)
# Add multiple keys to avoid rate limits (1 call per 15 seconds per key)
# POLLINATIONS_API_KEYS=your-api-key-1,your-api-key-2,your-api-key-3

# Other Configuration
# TOOLS_DIRECTORY_PATH=tools
# LOG_LEVEL=INFO
